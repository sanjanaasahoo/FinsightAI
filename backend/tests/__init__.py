"""
Test package for the FinSight AI backend.

This phase includes only a smoke test verifying the application boots,
the database connects, storage directories are created, and the health
endpoint responds correctly. Domain-specific test modules (analytics,
scenario, ML, RAG ingestion/retrieval, auth) will be added in later
phases per the architecture document's Testing Strategy.
"""
